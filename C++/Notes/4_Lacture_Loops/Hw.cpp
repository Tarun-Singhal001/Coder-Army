#include <iostream>
using namespace std;
int main()
{
    /*
    // Q-1   Two numbers are given, print the bigger number,it is giben that both numbers can't be the same.

    int a,b;
    cout<<"Enter value of a: ";
    cin>>a;
    cout<< "Enter value of b: ";
    cin>>b;
    if(a>b){
        cout<<a<<" is Greater then "<<b;
    }
    else{
        cout<<b<<" is Greater then "<<a;
    }
    */

    /*
    // Q-2  Age of a person is giben, print Adult if his/her age is greater than  18, otherwise print Teenager.

    int age;
    cout<<"Enter your age: ";
    cin>>age;
    if(age>18){
    cout<<"You are an Adult.";
    }
    else
    cout<<"You are an Teenager.";
    */

    /*
    // Q-3 Take a number in input (ex n) and print the corresponding month to it. Ex: for n=1, print january, n=2, print febuary like this you need to give output. It is given that n will be greater than 0 and less than 13.

    int month;
    cout<<"Enter no. ";
    cin>>month;
    if(month == 1){
        cout<<"January";
    }
    else if(month == 2){
        cout<<"Febuary";
    }
    else if(month == 3){
        cout<<"March";
    }
    else if(month == 4){
        cout<<"April";
    }
    else if(month == 5){
        cout<<"May";
    }
    else if(month == 6){
        cout<<"June";
    }
    else if(month == 7){
        cout<<"July";
    }
    else if(month == 8){
        cout<<"August";
    }
    else if(month == 9){
        cout<<"September";
    }
    else if(month == 10){
        cout<<"October";
    }
    else if(month == 11){
        cout<<"November";
    }
    else if(month == 12){
        cout<<"December";
    }
    else{
        cout<<"Enter no. between 1 to 13 ";
    }
    */
    /*
    // Q-4   Ticket price calculator: Create a program that asks the user for their age and checks if they qualify for a discounted ticket price (e.g., under 12 and over 65 get discounts), If they are eligible print "YES" else "NO".

    int age;
    cout<<"Enter Your age: ";
    cin>>age;
    if(age>=65 or age<=12)
    {
        cout<<"YES";
    }
    else cout<<"NO";
    */
    /*
    // Q-1 Print "India will win the World Cup of 2023", 20 times.

    int i;
    for(i=1; i<=20; i++)
    {
     cout<<i<<" India will win the World cup of 2023"<<endl;
    }
    */

    // Q-2   Print all Odd numbers from 1 to n, take n as an input from the user.
    /*
    int n, i;
    cout << "Give me a number: ";
    cin >> n;
    for (i = 1; i <= n; i++)
    {
        if (i % 2 != 0)
        {
            cout << i<< " ";
        }
    }
    */

    // Q-3 Print all numbers from 1 to n, which is dibisible by 4. Take n as an input from the user.
    /*
    int n;
    cout<<"Give me a number: ";
    cin>>n;
    for(int i=1;i<=n;i++){
        if(i%4==0){
            cout<<i<<" ";
        }
    }
    */
}